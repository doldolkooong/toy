[project]
name = "venv-toy1"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "doldolkooong", email = "912110h@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "jupyter>=1.1.1",
    "pandas>=3.0.5",
]

[project.scripts]
venv-toy1 = "venv_toy1:main"

[build-system]
requires = ["uv_build>=0.12.2,<0.13.0"]
build-backend = "uv_build"
